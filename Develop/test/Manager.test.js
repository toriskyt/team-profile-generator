const Employee = require(`../lib/Employee`);
const Manager = require(`../lib/Manager`);

// describe(`manager`, () => {
//     it(`should ask for manager name`, () => {
//         expect(manager([])).toBe(``);
//     })
// });

// it(`should return the employee position`, () => {
//     expect(manager([])).toBe("");
// })